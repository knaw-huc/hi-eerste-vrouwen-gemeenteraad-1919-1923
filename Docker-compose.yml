version: '3'

services:
  web:
    image: nginx
    volumes:
      - hi-eerste-vrouwen-gemeenteraad-1919-1923-html:/usr/share/nginx/html
    networks:
      - traefik-public
    labels:
      - "traefik.enable=true"
      - "traefik.http.services.religious-policies.loadbalancer.server.port=80"
      - "traefik.http.routers.religious-policies.entrypoints=http"
      - "traefik.http.routers.religious-policies.rule=Host(`eerste-vrouwen-gemeenteraad-1919-1923.huygens.knaw.nl`)"
  cron:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - hi-eerste-vrouwen-gemeenteraad-1919-1923-html:/app/repo/html

volumes:
  hi-eerste-vrouwen-gemeenteraad-1919-1923-html:

networks:
  traefik-public:
    external: true